name: pricing_front_page
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

  state_notifier: ^0.7.1
  freezed: ^1.0.0
  json_serializable: ^6.0.1
  
  #zwap packages
  zwap_design_system: ^0.1.357
  taastrap: ^0.1.29
  zwap_utils: ^0.0.42

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.5
  
flutter:
  uses-material-design: true
